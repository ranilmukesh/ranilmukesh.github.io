# Push to astro-ranilmukesh-website2026 (existing)
git push origin main

# Push to ranilmukesh.github.io (new)
git push pages main

# Push to BOTH at the same time
git push origin main && git push pages main